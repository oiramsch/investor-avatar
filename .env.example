ANTHROPIC_API_KEY=your_anthropic_api_key_here
DID_API_KEY=your_d_id_api_key_here
NOTION_TOKEN=your_notion_integration_token_here
NOTION_DATABASE_ID=37a765e5-dc96-80d8-a1ba-000b4c8c86d9

# Set by scripts/create_agent.py (D-ID Agent provisioning)
DID_AGENT_ID=
DID_CLIENT_KEY=

# Shared secret for D-ID → /api/llm Custom-LLM calls. Generate once with:
#   python -c 'import secrets; print(secrets.token_urlsafe(32))'
# Same value must be configured on the D-ID Agent (handled by create_agent.py).
LLM_ENDPOINT_KEY=

# Optional: D-ID presenter image URL (legacy /api/chat fallback)
# DID_PRESENTER_URL=https://create-images-results.d-id.com/DefaultPresenters/Noelle_f/v1_image.jpeg

# Optional: presenter configuration for scripts/create_agent.py
# PRESENTER_TYPE=expressive   # expressive (default) = LiveKit + mic-publish; "clip" or "talk" = WebRTC-only
# PRESENTER_ID=noelle-c2nQB6Cy11
# DRIVER_ID=uM00QMwJ9x        # only needed for PRESENTER_TYPE=clip or talk
# VOICE_ID=en-US-JennyMultilingualNeural
